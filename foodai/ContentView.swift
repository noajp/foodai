//======================================================================
// MARK: - ContentView.swift（修正版）
// Path: foodai/ContentView.swift
//======================================================================
import SwiftUI

struct ContentView: View {
    var body: some View {
        // シンプルにRootViewを表示（認証状態に応じて画面切り替え）
        RootView()
    }
}

#Preview {
    ContentView()
}
